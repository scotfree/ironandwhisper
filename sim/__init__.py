"""Iron and Whisper rules simulator.

The engine here is the executable specification of the rules in
../ironandwhisper.md, and reads the same JSON config the PHP implementation
will read, so parameter tuning done here transfers directly.
"""
